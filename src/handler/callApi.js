exports.handler = function(event, context) {

    // context would contain the SQS message

    // call API
        // if success
        // remove from SQS

        //if fails on rate limit
        // close circuit
}
